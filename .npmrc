# ⚠️⚠️⚠️ セキュリティ設定 - 編集禁止 ⚠️⚠️⚠️
# Shai-Huludマルウェア対策のため、以下の設定を維持してください

# 依存関係のバージョンを正確に固定
save-exact=true

# package-lock.jsonを必須化
package-lock=true

# セキュリティ監査を常に実行
audit=true

# ⚠️ 最重要: postinstallなどのスクリプト自動実行を無効化
# この設定を無効化しないこと！
ignore-scripts=true

# オプション指定を強制（安全性向上）
prefer-offline=true

# 進捗表示を有効化（何がインストールされているか確認）
progress=true

# 詳細ログを有効化
loglevel=warn

